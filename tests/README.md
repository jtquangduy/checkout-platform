# tests/

Cross-service tests that don't belong to any single service:

- `e2e/` — Playwright, the full brief scenario across all services and mocks
- `contract/` — Pact broker config and consumer expectations (see [`../docs/TESTING.md`](../docs/TESTING.md))
