# apps/

End-user applications. Currently one: `portal-web`, the React 19 + Vite + TS customer portal (see [`../docs/FRONTEND.md`](../docs/FRONTEND.md)). Deliberately built last, after the backend path works end to end.
